export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Login page URL for standalone authentication
export const getLoginUrl = () => {
  return "/login";
};

// Register page URL
export const getRegisterUrl = () => {
  return "/register";
};
